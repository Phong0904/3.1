#include<iostream>
using namespace std;
int nhapmang(int a[], int n) {
	for (int i = 0; i < n; i++) {
		cout << "Nhap vào a[" << i << "]: " << endl;
		cin >> a[i];
		if (a[i] == 0) {
			return 0;
		}
	}
	
}
void xuatmang(int a[], int n) {
	for (int i = 0; i < n; i++) {
		cout << a[i] << " ";
	}
}
void xapxep(int a[], int n) {
	for (int i = 0;i < n-1;i++) {
		for (int j = i;j < n;j++) {
			if (a[i] > a[j]) {
				int tam = a[i];
				a[i] = a[j];
				a[j] = tam;
			}
		}
	}
}
int main() {
	int a[1000];
	int n;
	cout << "Nhap n: " << endl;
	cin >> n;
	nhapmang(a , n);
	xapxep(a, n);
	cout << "mang la: " << endl; 
	xuatmang(a , n);
	return 0;
}
